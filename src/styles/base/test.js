// src/styles/base/test.js
// Path: src/styles/base/test.js

/**
 * Simple test file to verify base directory connection
 * No dependencies, just a basic export
 */

export const baseTest = {
  working: true,
  message: 'Base directory connected successfully!',
};

export function testBaseConnection() {
  return 'Base styles are connected and working!';
}

// 351 characters